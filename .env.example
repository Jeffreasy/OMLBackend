# Database configuratie
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=odomosml
# Zet op true om tabellen te droppen bij opstarten (alleen voor development!)
DB_DROP_TABLES=false

# JWT configuratie
JWT_SECRET=your-secret-key
JWT_EXPIRATION_HOURS=24

# Server configuratie
PORT=8080
GIN_MODE=debug  # Gebruik 'release' voor productie 